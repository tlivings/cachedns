{
  "name": "cachedns",
  "description": "Caching of DNS functions.",
  "version": "1.0.1",
  "author": "Trevor Livingston <tlivings@gmail.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git://github.com/tlivings/cachedns.git"
  },
  "bugs": "http://github.com/tlivings/cachedns/issues",
  "publishConfig": {
    "registry": "https://registry.npmjs.org"
  },
  "engines": {
    "node": ">=0.10.7"
  },
  "dependencies": {
    "babel-runtime": "^5.8.29",
    "core-util-is": "^1.0.1"
  },
  "devDependencies": {
    "babel": "^5.8.29",
    "glob": "^5.0.15",
    "hammertime": "^0.1.3",
    "tape": "^4.2.2"
  },
  "scripts": {
    "prepublish": "npm run compile",
    "test": "npm run compile && babel-node test/harness.js test/*.js",
    "compile": "babel --optional runtime --modules common --out-dir dist lib/**.js",
    "bench": "npm run compile && babel-node test/benchmark/lookup.js"
  },
  "main": "./dist/lib/index"
}
